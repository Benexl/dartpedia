# arg_parse

A custom Dart library for parsing command-line arguments and commands.

## Features
- Supports flags (`--help`, `-h`).
- Supports options with values (`--theme wikipedia`).
- Supports subcommands (`search`, `cache`).
- Command hierarchy with context sharing.
